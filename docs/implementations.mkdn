# Implementations

Complete or upcoming implementations of PAGI-compatible components — servers, frameworks, and other useful pieces. Contributions should include the project name, repository link, supported protocols, and status (prototype, beta, stable).

## Servers

- **PAGI::Server** — Reference IO::Async-based server with HTTP/1.1, HTTP/2, WebSocket, SSE, and TLS support. Features multi-worker pre-fork mode, async file streaming, graceful shutdown, and signal-based worker scaling. Optional performance optimizations include EV event loop and Future::XS. Protocols: `http`, `websocket`, `sse`, `lifespan`. Status: **stable**. [GitHub](https://github.com/jnapiorkowski/PAGI)

## Application Frameworks

- **Thunderhorse** — A no-compromises async web framework built on PAGI. Features controller-based architecture, WebSocket and SSE support, Template::Toolkit integration, middleware system, and context-based request handling. Protocols: `http`, `websocket`, `sse`. Status: **beta**. [MetaCPAN](https://metacpan.org/dist/Thunderhorse)

## Middleware & Apps

Bundled with PAGI::Server:

- **PAGI::Middleware::*** — 37 middleware components including GZip, CORS, CSRF, Session, RateLimit, Auth (Basic/Bearer), and more.
- **PAGI::App::*** — 17 reusable apps including File (static serving), Directory (listings), Proxy, URLMap, WrapPSGI, and WebSocket/SSE utilities.

## Tools / Utilities

- **pagi-server** — CLI for running PAGI applications. Supports daemonization, PID files, privilege dropping, TLS/SSL, HTTP/2 (`--http2`), multi-worker mode, and graceful restarts via signals. Optional `--future-xs` flag for ~3% HTTP/1.1 performance boost. Bundled with PAGI::Server.
